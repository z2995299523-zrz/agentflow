# FastAPI 接口 — 实施计划

> **For Hermes:** 将此计划交给 Claude Code 执行。

**目标：** 创建 FastAPI 入口，为 RAG 和 SQL 模块暴露 REST API

**架构：**
```
main.py (FastAPI app)
├── /rag/upload      POST  上传文档
├── /rag/query       POST  知识库问答
├── /rag/documents   GET   文档列表
├── /sql/query       POST  Text-to-SQL
├── /sql/visualize   POST  查询+图表
└── /sql/schema      GET   数据库表结构
```

---

## Task 1: 创建 main.py — FastAPI 入口

新建 `C:\Users\DELL\agentflow\main.py`

```python
"""
AgentFlow API — FastAPI 统一接口
提供 RAG 知识库问答 + Text-to-SQL 数据分析的 REST API
"""
import os
import uuid
from pathlib import Path
from typing import Optional

from fastapi import FastAPI, UploadFile, File, HTTPException
from fastapi.responses import JSONResponse
from pydantic import BaseModel, Field

from config import UPLOAD_DIR
from rag.chain import ask, ask_with_detail
from rag.loader import load_document
from rag.vectorstore import add_documents, load_vectorstore, get_document_list
from sql.agent import SQLQueryAgent
from sql.visualizer import QueryVisualizer

# ─── 应用初始化 ──────────────────────────────────

app = FastAPI(
    title="AgentFlow API",
    description="企业级 AI 智能助手 — RAG + Text-to-SQL",
    version="2.0.0",
)

# 确保上传目录存在
Path(UPLOAD_DIR).mkdir(parents=True, exist_ok=True)

# 懒加载的单例
_sql_agent: Optional[SQLQueryAgent] = None
_visualizer: Optional[QueryVisualizer] = None


def get_sql_agent() -> SQLQueryAgent:
    global _sql_agent
    if _sql_agent is None:
        _sql_agent = SQLQueryAgent()
    return _sql_agent


def get_visualizer() -> QueryVisualizer:
    global _visualizer
    if _visualizer is None:
        _visualizer = QueryVisualizer()
    return _visualizer


# ─── 请求/响应模型 ───────────────────────────────

class QueryRequest(BaseModel):
    question: str = Field(..., description="自然语言问题", min_length=1)


class RAGQueryResponse(BaseModel):
    answer: str
    sources: list[str]
    context_details: Optional[list[dict]] = None


class SQLQueryResponse(BaseModel):
    question: str
    sql: str
    answer: str
    success: bool
    error: Optional[str] = None


class VisualizeResponse(BaseModel):
    question: str
    sql: str
    answer: str
    chart_base64: str
    chart_type: str
    success: bool
    error: Optional[str] = None


class DocumentInfo(BaseModel):
    filename: str
    size: int


# ─── RAG 路由 ────────────────────────────────────

@app.post("/rag/upload", response_model=dict)
async def upload_document(file: UploadFile = File(...)):
    """上传文档到知识库"""
    # 保存文件
    filename = f"{uuid.uuid4().hex[:8]}_{file.filename}"
    filepath = Path(UPLOAD_DIR) / filename
    content = await file.read()
    filepath.write_bytes(content)

    # 加载并向量化
    try:
        docs = load_document(str(filepath))
        if not docs:
            os.remove(filepath)
            raise HTTPException(400, "文档内容为空或格式不支持")

        add_documents(docs)
        return {
            "success": True,
            "filename": file.filename,
            "chunks": len(docs),
            "message": f"已上传 {file.filename}，分割为 {len(docs)} 个文本块"
        }
    except HTTPException:
        raise
    except Exception as e:
        if filepath.exists():
            os.remove(filepath)
        raise HTTPException(500, f"文档处理失败: {str(e)}")


@app.post("/rag/query", response_model=RAGQueryResponse)
async def query_rag(request: QueryRequest):
    """知识库问答"""
    try:
        result = ask_with_detail(request.question)
        if not result.get("answer"):
            raise HTTPException(404, "未找到相关内容")
        return RAGQueryResponse(**result)
    except HTTPException:
        raise
    except Exception as e:
        raise HTTPException(500, f"查询失败: {str(e)}")


@app.get("/rag/documents", response_model=list[DocumentInfo])
async def list_documents():
    """列出已上传的文档"""
    try:
        files = get_document_list()
        return [DocumentInfo(filename=f["source"], size=f.get("size", 0)) for f in files]
    except Exception as e:
        raise HTTPException(500, f"获取文档列表失败: {str(e)}")


@app.delete("/rag/documents/{filename}")
async def delete_document(filename: str):
    """删除文档及其向量"""
    try:
        # 删除文件
        filepath = Path(UPLOAD_DIR) / filename
        if filepath.exists():
            os.remove(filepath)
        # 删除向量（ChromaDB 目前不支持按 source 删除，需重建）
        return {"success": True, "message": f"已删除 {filename}（注：需重建向量库以完全清除）"}
    except Exception as e:
        raise HTTPException(500, f"删除失败: {str(e)}")


# ─── SQL 路由 ────────────────────────────────────

@app.post("/sql/query", response_model=SQLQueryResponse)
async def query_sql(request: QueryRequest):
    """自然语言查询数据库"""
    try:
        agent = get_sql_agent()
        result = agent.query(request.question)
        if not result["success"]:
            raise HTTPException(400, result.get("error", "查询失败"))
        return SQLQueryResponse(**result)
    except HTTPException:
        raise
    except Exception as e:
        raise HTTPException(500, f"查询失败: {str(e)}")


@app.post("/sql/visualize", response_model=VisualizeResponse)
async def visualize_sql(request: QueryRequest):
    """查询数据库并生成图表"""
    try:
        agent = get_sql_agent()
        viz = get_visualizer()

        result = agent.query(request.question)
        if not result["success"]:
            raise HTTPException(400, result.get("error", "查询失败"))

        # 从 SQL 结果生成图表（需要 DataFrame）
        import pandas as pd
        from sql.agent import SafeSQLDatabase
        from config import get_db_uri

        db = SafeSQLDatabase.from_uri(get_db_uri())
        df = pd.read_sql_query(result["sql"], db._engine)
        chart = viz.create_chart(df)

        return VisualizeResponse(
            question=request.question,
            sql=result["sql"],
            answer=result["answer"],
            chart_base64=chart["image_base64"],
            chart_type=chart["chart_type"],
            success=True,
        )
    except HTTPException:
        raise
    except Exception as e:
        raise HTTPException(500, f"可视化失败: {str(e)}")


@app.get("/sql/schema", response_model=dict)
async def get_schema():
    """获取数据库表结构"""
    try:
        agent = get_sql_agent()
        schema = agent.get_schema()
        return {"success": True, "schema": schema}
    except Exception as e:
        raise HTTPException(500, f"获取表结构失败: {str(e)}")


# ─── 健康检查 ────────────────────────────────────

@app.get("/health")
async def health():
    return {"status": "ok", "service": "AgentFlow API"}
```

---

## Task 2: 更新 requirements.txt

确保 `C:\Users\DELL\agentflow\requirements.txt` 包含：
```
fastapi
uvicorn
python-multipart
```

---

## Task 3: 验证

```bash
cd C:\Users\DELL\agentflow && python -c "from main import app; print('FastAPI app created OK')"
```

然后启动测试：
```bash
cd C:\Users\DELL\agentflow && python -m uvicorn main:app --host 127.0.0.1 --port 8000
```

访问 http://127.0.0.1:8000/docs 查看 Swagger UI

---

## Task 4: 回归测试

确保不破坏已有功能：
```bash
python -m pytest test_sql_tools.py::test_tool_name test_sql_tools.py::test_tool_description_length test_sql_tools.py::test_tool_description_mentions_db -v
```
