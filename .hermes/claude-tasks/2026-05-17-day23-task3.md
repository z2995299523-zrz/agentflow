AgentFlow — Day 23-25 Task 3: 多轮对话记忆

## 项目上下文
- 工作目录: C:\Users\DELL\agentflow
- LangGraph 1.1.3, MemorySaver 可用
- 现有: graph/workflow.py 中的 build_workflow() 没有 checkpointer

## 任务
为 LangGraph 工作流加入多轮对话记忆能力：

### 1. 修改 graph/workflow.py — build_workflow 加入 MemorySaver

```python
from langgraph.checkpoint.memory import MemorySaver

def build_workflow(enable_memory: bool = True):
    """构建并编译多Agent 工作流
    
    Args:
        enable_memory: 是否启用多轮对话记忆（默认 True）
    """
    graph = StateGraph(AgentState)
    # ... 添加节点和边（不变）...
    
    if enable_memory:
        memory = MemorySaver()
        return graph.compile(checkpointer=memory)
    return graph.compile()
```

### 2. 创建 graph/memory.py — 对话历史管理工具

```python
"""多轮对话记忆管理 — MemorySaver + thread_id 隔离"""

from langgraph.checkpoint.memory import MemorySaver

# 全局 MemorySaver 单例
_memory = None

def get_memory() -> MemorySaver:
    """获取全局 MemorySaver 单例"""
    global _memory
    if _memory is None:
        _memory = MemorySaver()
    return _memory

def make_config(thread_id: str = "default") -> dict:
    """创建带 thread_id 的 config（用于区分不同用户/会话）
    
    Args:
        thread_id: 会话标识，如 "user-123" 或 "session-abc"
    
    Returns:
        {"configurable": {"thread_id": "..."}}
    """
    return {"configurable": {"thread_id": thread_id}}

def clear_memory(thread_id: str = "default") -> None:
    """清除指定会话的记忆（重置对话）"""
    # MemorySaver 不直接支持按 thread_id 清除，
    # 简单方案：新建 MemorySaver 实例
    global _memory
    _memory = MemorySaver()
```

### 3. 修改 graph/workflow.py — build_workflow 使用共享 MemorySaver

```python
from graph.memory import get_memory

def build_workflow(enable_memory: bool = True):
    graph = StateGraph(AgentState)
    # ... 添加节点和边 ...
    
    if enable_memory:
        return graph.compile(checkpointer=get_memory())
    return graph.compile()
```

### 4. 更新 graph/__init__.py 导出

```python
from graph.state import AgentState
from graph.workflow import build_workflow
from graph.memory import make_config, clear_memory

__all__ = ["AgentState", "build_workflow", "make_config", "clear_memory"]
```

### 5. 在 test_graph.py 添加 2 个记忆测试

```python
# 测试 11: 多轮对话记忆
def test_memory_multiround():
    """验证同一 thread_id 的多轮对话能记住上下文"""
    from graph.workflow import build_workflow
    from graph.memory import make_config
    
    wf = build_workflow(enable_memory=True)
    config = make_config("test-user-1")
    
    # 第一轮
    r1 = wf.invoke({"question": "你好"}, config)
    assert "final_answer" in r1
    
    # 第二轮（同样的 thread_id）
    r2 = wf.invoke({"question": "我刚才说了什么"}, config)
    assert "final_answer" in r2
    
    # 两轮的消息历史应该不同
    assert len(r2.get("messages", [])) > len(r1.get("messages", []))

# 测试 12: 不同 thread_id 隔离
def test_memory_isolation():
    """验证不同 thread_id 的对话不会串"""
    from graph.workflow import build_workflow
    from graph.memory import make_config
    
    wf = build_workflow(enable_memory=True)
    
    r_a = wf.invoke({"question": "产品表有多少条"}, make_config("user-A"))
    r_b = wf.invoke({"question": "你好"}, make_config("user-B"))
    
    # 两个用户的消息应该独立
    assert "final_answer" in r_a
    assert "final_answer" in r_b
```

## 关键约束
- 不破坏现有代码
- MemorySaver 是内存级，重启丢失（够用，不引入 SqliteSaver）
- messages 字段用 add_messages reducer，自动累积历史
- make_config 返回 dict，不是复杂对象

## 验证
1. python -c "from graph import make_config; print(make_config('test'))"
2. python -m pytest test_graph.py -v -k "test_memory" --tb=short
3. python -m pytest test_graph.py -v -k "test_workflow_build" --tb=short
