AgentFlow — Day 23-25 Task 1+2: BGE模型预热 + Mixed双路并行

## 项目上下文
- 工作目录: C:\Users\DELL\agentflow
- Python 3.12+, LangChain 1.x, LangGraph 1.1.3
- 现有模块: rag/, sql/, graph/, prompts/, config.py, main.py

## Task 1: BGE 模型预热 (main.py + graph/nodes.py)

### 目标
把 BGE 模型从"第一次请求等15秒"改为"FastAPI 启动时预热"

### 修改 main.py
在 lifespan 事件中预热模型，注入到 graph 节点：

```python
# main.py 顶部加 import
from contextlib import asynccontextmanager

# 替换 app = FastAPI(...) 为:
@asynccontextmanager
async def lifespan(app: FastAPI):
    """启动预热：加载 BGE 模型 + SQL Agent"""
    print("⏳ 预热 BGE 模型...")
    from rag.vectorstore import load_vectorstore
    app.state.vectorstore = load_vectorstore()
    print("✅ BGE 模型就绪")
    
    print("⏳ 预热 SQL Agent...")
    from sql.agent import SQLQueryAgent
    app.state.sql_agent = SQLQueryAgent()
    print("✅ SQL Agent 就绪")
    
    # 注入到 graph 节点
    from graph.nodes import init_vectorstore, init_sql_agent
    init_vectorstore(app.state.vectorstore)
    init_sql_agent(app.state.sql_agent)
    
    yield

app = FastAPI(title="AgentFlow API", lifespan=lifespan)
```

### 修改 graph/nodes.py
1. 把全局单例提到模块顶层，添加 init 函数
2. rag_node 和 sql_node 改为优先用注入的单例，fallback 懒加载

python
# nodes.py 顶部加
_vectorstore = None
_sql_agent = None

def init_vectorstore(vs):
    """注入预热的 vectorstore（由 main.py lifespan 调用）"""
    global _vectorstore
    _vectorstore = vs

def init_sql_agent(agent):
    """注入预热的 SQL agent"""
    global _sql_agent
    _sql_agent = agent

# rag_node 修改：
# 原来: from rag.vectorstore import load_vectorstore; vectorstore = load_vectorstore()
# 改为:
def rag_node(state):
    global _vectorstore
    question = state.get("question", "")
    if not question.strip():
        return {"rag_result": "问题为空", "rag_sources": []}
    try:
        from rag.chain import ask
        if _vectorstore is None:
            from rag.vectorstore import load_vectorstore
            _vectorstore = load_vectorstore()
        # 检查是否为空...
        result = ask(question, vectorstore=_vectorstore)
        ...

# sql_node 修改：
# 原来: from sql.agent import SQLQueryAgent; agent = SQLQueryAgent()
# 改为:
def sql_node(state):
    global _sql_agent
    question = state.get("question", "")
    if not question.strip():
        return {"sql_result": "问题为空", "sql_query": ""}
    try:
        if _sql_agent is None:
            from sql.agent import SQLQueryAgent
            _sql_agent = SQLQueryAgent()
        result = _sql_agent.query(question)
        ...
```

## Task 2: Mixed 意图双路并行 (graph/workflow.py)

### 目标
当 supervisor 判断 intent=mixed 时，走 RAG→SQL→finalize 路径（先查文档提取条件，再查数据库验证）

### 图结构调整
现在: supervisor → [rag|sql|fallback] → finalize → END
改后: supervisor → rag → [条件: mixed→sql, 其他→finalize] → finalize → END

### 修改 graph/workflow.py
在 finalize_node 和 fallback_node 之后添加新函数：

```python
def route_after_rag(state: AgentState) -> str:
    """RAG 节点后的路由：mixed 意图继续走 SQL，否则直接汇总"""
    if state.get("intent") == "mixed":
        return "sql"
    return "finalize"
```

修改 build_workflow():
```python
def build_workflow():
    graph = StateGraph(AgentState)
    graph.add_node("supervisor", supervisor_node)
    graph.add_node("rag", rag_node)
    graph.add_node("sql", sql_node)
    graph.add_node("finalize", finalize_node)
    graph.add_node("fallback", fallback_node)
    
    graph.add_edge(START, "supervisor")
    graph.add_conditional_edges("supervisor", route_by_intent, {
        "rag": "rag",
        "sql": "sql",
        "fallback": "fallback",
    })
    # 改：RAG 后加条件路由（原来是 graph.add_edge("rag", "finalize")）
    graph.add_conditional_edges("rag", route_after_rag, {
        "sql": "sql",
        "finalize": "finalize",
    })
    graph.add_edge("sql", "finalize")
    graph.add_edge("finalize", END)
    graph.add_edge("fallback", END)
    
    return graph.compile()
```

### 增强 sql_node：读取 RAG 上下文
在 sql_node 中，如果 state 有 rag_result，将其作为上下文注入问题：

```python
def sql_node(state):
    global _sql_agent
    question = state.get("question", "")
    if not question.strip():
        return {"sql_result": "问题为空", "sql_query": ""}
    
    # 如果有 RAG 结果，作为上下文注入
    rag_context = state.get("rag_result", "")
    if rag_context and state.get("intent") == "mixed":
        question = f"参考信息：{rag_context[:500]}\n\n问题：{question}"
    
    try:
        if _sql_agent is None:
            from sql.agent import SQLQueryAgent
            _sql_agent = SQLQueryAgent()
        result = _sql_agent.query(question)
        ...
```

### 修改 test_graph.py
添加两个新测试：
1. test_mixed_route_after_rag — 验证 intent=mixed 时 route_after_rag 返回 "sql"
2. test_workflow_mixed — 端到端测试 mixed 意图，验证 final_answer 包含 RAG 和 SQL 结果

## 关键约束
- 不破坏现有代码逻辑
- 所有函数保持 try/except
- 不要改 graph/state.py 和 graph/supervisor.py
- url: 不要改 graph/__init__.py 的导出

## 验证步骤
1. python -c "from graph import build_workflow; wf = build_workflow(); print('OK')"
2. python -m pytest test_graph.py -v -k "test_workflow_build or test_agent_state_creation" --tb=short
3. python -c "from graph.workflow import route_after_rag; s={'intent':'mixed'}; print(route_after_rag(s))"  # 应输出 sql
4. python -m pytest test_graph.py -v --tb=short  # 全量测试
